crypto_platform:
  target: dev
  outputs:
    dev:
      type: trino
      host: trino
      port: 8080
      user: dbt
      database: iceberg
      schema: staging
      threads: 2
      http_scheme: http
      session_properties:
        query_max_run_time: 5m
