# Iceberg catalog (backed by Hive Metastore on Postgres)
spark.sql.extensions                          org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions
spark.sql.catalog.iceberg                     org.apache.iceberg.spark.SparkCatalog
spark.sql.catalog.iceberg.type                hive
spark.sql.catalog.iceberg.uri                 thrift://hive-metastore:9083
spark.sql.catalog.iceberg.warehouse           s3a://warehouse/

# S3A filesystem (MinIO). Credentials match .env.example defaults; override via
# --conf on spark-submit if rotated.
spark.hadoop.fs.s3a.endpoint                  http://minio:9000
spark.hadoop.fs.s3a.access.key                minioadmin
spark.hadoop.fs.s3a.secret.key                minioadmin123
spark.hadoop.fs.s3a.path.style.access         true
spark.hadoop.fs.s3a.connection.ssl.enabled    false
spark.hadoop.fs.s3a.aws.credentials.provider  org.apache.hadoop.fs.s3a.SimpleAWSCredentialsProvider
spark.hadoop.fs.s3a.impl                      org.apache.hadoop.fs.s3a.S3AFileSystem

# Resource defaults tuned for a small dev box.
spark.driver.memory                           512m
spark.executor.memory                         1g
spark.executor.cores                          1
spark.sql.shuffle.partitions                  4

spark.sql.streaming.metricsEnabled            true
spark.sql.adaptive.enabled                    true
