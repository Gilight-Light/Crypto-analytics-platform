coordinator=true
node-scheduler.include-coordinator=true
http-server.http.port=8080
discovery.uri=http://localhost:8080

# Memory sizing for a dev laptop — keep Trino footprint under ~1.2GB.
query.max-memory=500MB
query.max-memory-per-node=300MB
memory.heap-headroom-per-node=150MB
